{
  "name": "kindline-guide",
  "private": true,
  "type": "module",
  "description": "Bounded, uncertainty-aware support tool for reflective coping practice on Cloudflare Pages + Workers AI.",
  "scripts": {
    "dev": "wrangler pages dev public --ai AI",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
